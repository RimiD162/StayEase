<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="Stay-Ease Admin Registry.">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&family=Outfit:wght@500;700;900&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/auth.css">
  <title>Admin Registration — Stay-Ease</title>
</head>
<body class="auth-body admin-theme">

  <div class="auth-container">
    <!-- ===== Decorative Left Panel ===== -->
    <div class="auth-sidebar">
      <a href="/" class="auth-sidebar-logo">
        Stay<span>Ease</span>
      </a>
      <div class="auth-sidebar-content">
        <h1 class="auth-sidebar-title">
          Admin Portal<br>
          <span class="highlight">Add Administrator</span>
        </h1>
        <p class="auth-sidebar-text">
          Authorized registration for Stay-Ease portal administration. A secret code is required to register.
        </p>
      </div>
      <div class="auth-sidebar-footer">
        &copy; 2026 Stay-Ease. Admin System.
      </div>
    </div>

    <!-- ===== Form Right Panel ===== -->
    <div class="auth-content">
      <div class="auth-card" id="auth-card-element" style="max-width: 500px; margin: 20px 0;">
        <div class="auth-header">
          <a href="/" class="auth-logo-mobile">
            Stay<span>Ease</span>
          </a>
          <h2 class="auth-title">Admin Registry</h2>
          <p class="auth-subtitle">Authorized registration only.</p>
        </div>

        <!-- Success or Error Alerts -->
        <% if (typeof error !== 'undefined' && error) { %>
          <div class="alert-banner alert-banner-error" id="server-error-banner">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <circle cx="12" cy="12" r="10"/>
              <line x1="12" y1="8" x2="12" y2="12"/>
              <line x1="12" y1="16" x2="12.01" y2="16"/>
            </svg>
            <span><%= error %></span>
          </div>
        <% } %>

        <form id="admin-signup-form" class="auth-form" action="/admin/signup" method="POST">
          
          <!-- Full Name -->
          <div class="form-group" id="group-fullName">
            <div class="input-icon-wrapper">
              <div class="input-icon">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/></svg>
              </div>
              <input type="text" class="form-input" id="fullName-input" name="fullName" placeholder=" " required value="<%= typeof oldValues !== 'undefined' ? oldValues.fullName : '' %>">
              <label class="form-label" for="fullName-input">Full Name *</label>
              <div class="input-focus-line"></div>
            </div>
            <div class="error-msg">Full name is required.</div>
          </div>

          <!-- Admin Username -->
          <div class="form-group" id="group-username">
            <div class="input-icon-wrapper">
              <div class="input-icon">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                  <path d="M20 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/>
                  <circle cx="9" cy="7" r="4"/>
                </svg>
              </div>
              <input type="text" class="form-input" id="username-input" name="username" placeholder=" " required value="<%= typeof oldValues !== 'undefined' ? oldValues.username : '' %>">
              <label class="form-label" for="username-input">Admin Username *</label>
              <div class="input-focus-line"></div>
            </div>
            <div class="error-msg">Admin username is required.</div>
          </div>

          <!-- Email Address -->
          <div class="form-group" id="group-email">
            <div class="input-icon-wrapper">
              <div class="input-icon">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/><polyline points="22,6 12,13 2,6"/></svg>
              </div>
              <input type="email" class="form-input" id="email-input" name="email" placeholder=" " required value="<%= typeof oldValues !== 'undefined' ? oldValues.email : '' %>">
              <label class="form-label" for="email-input">Email Address *</label>
              <div class="input-focus-line"></div>
            </div>
            <div class="error-msg">A valid email address is required.</div>
          </div>

          <!-- Phone Number -->
          <div class="form-group" id="group-phone">
            <div class="input-icon-wrapper">
              <div class="input-icon">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                  <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/>
                </svg>
              </div>
              <input type="tel" class="form-input" id="phone-input" name="phone" placeholder=" " value="<%= typeof oldValues !== 'undefined' ? oldValues.phone : '' %>">
              <label class="form-label" for="phone-input">Phone Number</label>
              <div class="input-focus-line"></div>
            </div>
          </div>

          <!-- Secret Admin Code -->
          <div class="form-group" id="group-secretCode">
            <div class="input-icon-wrapper">
              <div class="input-icon">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                  <rect x="3" y="11" width="18" height="11" rx="2" ry="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/>
                </svg>
              </div>
              <input type="text" class="form-input" id="secretCode-input" name="secretCode" placeholder=" " required>
              <label class="form-label" for="secretCode-input">Secret Admin Code *</label>
              <div class="input-focus-line"></div>
            </div>
            <div class="error-msg">Secret admin code is required.</div>
          </div>

          <!-- Submit Button -->
          <button type="submit" class="btn-auth-submit">
            Register Administrator
          </button>

          <!-- Back link -->
          <a href="/admin/login" class="auth-back-link">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <line x1="19" y1="12" x2="5" y2="12"/><polyline points="12 19 5 12 12 5"/>
            </svg>
            Back to Login
          </a>
        </form>
      </div>
    </div>
  </div>

  <script>
    const form = document.getElementById('admin-signup-form');
    const card = document.getElementById('auth-card-element');
    const serverError = document.getElementById('server-error-banner');

    if (serverError) {
      card.classList.add('shake-field');
      setTimeout(() => card.classList.remove('shake-field'), 500);
    }

    form.addEventListener('submit', function(e) {
      let hasError = false;

      // Validate Full Name
      const fullNameVal = document.getElementById('fullName-input').value.trim();
      if (!fullNameVal) {
        document.getElementById('group-fullName').classList.add('has-error');
        hasError = true;
      }

      // Validate Username
      const usernameVal = document.getElementById('username-input').value.trim();
      if (!usernameVal) {
        document.getElementById('group-username').classList.add('has-error');
        hasError = true;
      }

      // Validate Email
      const emailVal = document.getElementById('email-input').value.trim();
      const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
      if (!emailVal || !emailRegex.test(emailVal)) {
        document.getElementById('group-email').classList.add('has-error');
        hasError = true;
      }

      // Validate Secret Code
      const secretCodeVal = document.getElementById('secretCode-input').value.trim();
      if (!secretCodeVal) {
        document.getElementById('group-secretCode').classList.add('has-error');
        hasError = true;
      }

      if (hasError) {
        e.preventDefault();
        card.classList.add('shake-field');
        setTimeout(() => card.classList.remove('shake-field'), 500);
      }
    });

    // Remove errors on input
    document.querySelectorAll('.form-input').forEach(input => {
      input.addEventListener('input', function() {
        this.closest('.form-group').classList.remove('has-error');
      });
    });
  </script>
</body>
</html>
